#include <stdio.h>
#include <stdlib.h>

int main()
{
    const float PI = 3.141592653589793238; // constant variable named PI
    // PI = 3.14 - error as constant variable cannot be modified
    printf("Hello Constants\n"); // It can also be called constants, since it cannot be modified

    printf("%d\n", 97); // Here the number 97 is a Constant, as it cannot be modified

    return 0;
}